# review

This directory is populated automatically by the **build-markdown** workflow
when it is triggered with `mode: review`.

The workflow converts `paper/paper.md` to `paper.html` and `paper.pdf` and
places them here so that AI Reviewer and AI Editor bots can inspect the
rendered output.
